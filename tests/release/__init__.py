"""Release suite: scripted PTY scenarios against a fresh `testdata/kvstore` repo built per
capture (`tests/release/kvstore_repo.py`) with the live Claude Agent SDK
(`BLARE_SDK_FIXTURES=record:<dir>`), one per entry on `engineering/modules/agent.md`'s
provisional-fixtures list (T4.1)."""
