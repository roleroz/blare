"""Release suite: scripted PTY scenarios against `~/external_git/miniflux_v2` with the
live Claude Agent SDK (`BLARE_SDK_FIXTURES=record:<dir>`), one per entry on
`engineering/modules/agent.md`'s provisional-fixtures list (T4.1)."""
