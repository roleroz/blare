{
  "fast": ".venv/bin/ruff check src tests pytest_main.py && .venv/bin/mypy --strict src tests pytest_main.py && bazel test --test_tag_filters=-e2e,-live,-quarantined //...",
  "full": ".venv/bin/ruff check src tests pytest_main.py && .venv/bin/mypy --strict src tests pytest_main.py && bazel test --test_tag_filters=-live,-quarantined //...",
  "release": "bazel test --test_tag_filters=live $(bazel query \"attr('tags', 'live', //tests/release/...)\")"
}
